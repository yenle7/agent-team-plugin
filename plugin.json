{
  "name": "agent-team",
  "version": "0.1.0",
  "description": "A small-startup-team of Claude Code subagents that take a consumer-app feature from Think to Reflect, logging every phase to Notion.",
  "author": "Yen",
  "license": "MIT",
  "agents_dir": "agents",
  "hooks_dir": "hooks",
  "scripts_dir": "scripts",
  "requires": {
    "mcp": ["notion"],
    "claude_code_min_version": "1.0.0"
  },
  "notes": "Verify the exact plugin.json schema against the current Claude Code plugin docs before publishing — the schema is evolving. This manifest is intentionally minimal."
}
