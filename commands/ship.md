---
description: Use AFTER /qa is clean (or all bugs resolved by /investigate). Mechanical release work — opens the PR with a populated description, wraps the change in a feature flag, registers the analytics event...
argument-hint: <ticket id>
---
Use the **ship** subagent to handle this request.

User input: $ARGUMENTS
