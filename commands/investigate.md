---
description: Use when /qa or a user surfaces a non-trivial bug. Systematic root-cause debugging. Iron Law — no fixes without investigation. Traces data flow, tests hypotheses, stops after 3 failed fixes. Can im...
argument-hint: <bug id or description>
---
Use the **investigate** subagent to handle this request.

User input: $ARGUMENTS
