---
description: Use 7 days AFTER /ship (or whenever the feature has been live long enough to have usage data). Closes the loop — reads the original /plan, the PR stats, the bug log, and 7-day flag usage data, then...
argument-hint: <ticket id>
---
Use the **reflect** subagent to handle this request.

User input: $ARGUMENTS
