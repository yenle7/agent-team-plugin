---
description: Use AFTER /plan, /plan-eng-review, and /plan-design-review have run. The implementer. Reads plan + eng-review + design, runs a strict pre-flight to surface unknowns, then writes code in atomic comm...
argument-hint: <ticket id>
---
Use the **build** subagent to handle this request.

User input: $ARGUMENTS
