---
description: Use AFTER /plan (can run in parallel with /plan-eng-review). Builds a complete design direction for the feature — research, creative risks, realistic mockups, design tokens, component specs. Reads ...
argument-hint: [ticket id]
---
Use the **designer** subagent to handle this request.

User input: $ARGUMENTS
